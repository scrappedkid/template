import styled from 'styled-components'
import WithDirection from '@root/settings/withdirection'

const MailDrawer = styled.div`
  > div > div {
    background-color: #ffffff;
  }
`

export default WithDirection(MailDrawer)
